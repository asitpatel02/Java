/**
 * An interface for a position, a data structure representing a
 * position in a list.
 */

  public interface Position<E> {
    /**
     * Return the element stored at this position.
     */
     E element();
  }